#include "layout_engine.hpp"

namespace demo {
    void demo(LayoutEngine::LayoutEngine& layout_engine);

    void button(const std::string& id, LayoutEngine::LayoutEngine& layout_engine);
    void checkbox(const std::string& id, LayoutEngine::LayoutEngine& layout_engine);
};
